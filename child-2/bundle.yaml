apiVersion: "1"
id: "child-2"
description: "Child-2 CasC Setup"
version: "3"
parent: "child-1"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
#items:
#  - "items.yaml"
# Commenting out RBAC since we're not using for this demo
#rbac:
#  - "rbac.yaml"
# Commenting out Catalog since we're not using for this demo
catalog:
  - "plugin-catalog.yaml"
variables:
  - "variables.yaml"